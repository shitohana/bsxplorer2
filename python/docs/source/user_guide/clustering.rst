Clustering workflows
====================

The clustering pipeline builds gene profile matrices from BSX and annotation
inputs and supports:

- PCA embeddings
- k-means clustering
- hierarchical dendrograms
- cluster metagene summaries

For interactive exploration, use the plot studio. For scripted pipelines, use
the builders and composers in ``bsx2.clustering`` and ``bsx2.viz`` directly.
