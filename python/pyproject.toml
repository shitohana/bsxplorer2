[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "bsx2"
version = "0.1.0"
requires-python = ">=3.9,<3.12"
dependencies = [
    "polars",
    "pyarrow"
]

[tool.pyright]
venvPath = "."
venv = ".venv"
include = ["bsx2"]
exclude = ["src"]
stubPath = "./typings"
